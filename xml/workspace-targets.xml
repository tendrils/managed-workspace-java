<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:j2seproject1="http://www.netbeans.org/ns/j2se-project/1"
         xmlns:j2seproject3="http://www.netbeans.org/ns/j2se-project/3"
         xmlns:jaxrpc="http://www.netbeans.org/ns/j2se-project/jax-rpc"
         basedir=".." default="default" name="workspace-tasks">
    <!--
        ant/workspace-targets.xml
        *******************
        defines ant targets to automate workspace management
        and housekeeping tasks.
        
        
        
        
        
    -->
    <!--
        begin configuration targets
    -->
    <target name="configure-workspace"></target>
    <target name="init-workspace" depends="configure-workspace">
    </target>
    
    <!-- 
        end configuration targets
    -->
    <!-- ****************** -->
    <!--
        begin workspace targets
    -->
    <target name="update" depends="init-workspace" description="load environment">
        
    </target>
    <!-- 
        end workspace targets
    -->
    <!-- ****************** -->
    <!-- 
        begin project targets
    -->
    <target name="new-project" depends="init-workspace">
        <
    </target>


    <!--
        end project targets
    -->
    <!-- ****************** -->
    <!-- 
        begin package targets
    -->
    <target name="pkg-info" depends="init-workspace">
    
    </target>
    <target name="pkg-get" depends="init-workspace,have-repo">
        
    </target>
    <!-- 
        end package targets
    -->
</project>